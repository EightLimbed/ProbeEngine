// common constants
const uint chunkSize = 64u;
const uint viewSize = 8u;

const int raySteps = 256;
const float renderDist = 512.0;
const float e = 0.01; // max precision of 8 bit float
const float dMin = -sqrt(2.0); // important to allow negative sdf distance data encoding.


const uint chunkProbes = chunkSize*chunkSize*chunkSize;
const uint viewChunks = viewSize*viewSize*viewSize;
const uint totalProbes = viewChunks*chunkProbes;
const uint probeUints = totalProbes/4;
const uint materialUints = totalProbes/8;